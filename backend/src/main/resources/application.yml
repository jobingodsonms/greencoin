spring:
  application:
    name: greencoin-backend
  
  datasource:
    driver-class-name: org.postgresql.Driver
    # The URL, username, and password are handled by DatabaseConfig.java
    # to support Railway's DATABASE_URL format.

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# Firebase Configuration
firebase:
  project-id: ${FIREBASE_PROJECT_ID:greencoin-bc425}
  credentials-path: ${FIREBASE_CREDENTIALS_PATH:firebase-admin-key.json}

# Server Configuration
server:
  port: ${PORT:8080}
  error:
    include-message: always

# Management Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# CORS Configuration
cors:
  allowed-origins: ${ALLOWED_ORIGINS:https://greencoin-chi.vercel.app,http://localhost:3000}
