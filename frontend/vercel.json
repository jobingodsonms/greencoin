{
    "buildCommand": "echo 'No build needed for static site'",
    "outputDirectory": ".",
    "installCommand": "echo 'No dependencies to install'",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/$1"
        }
    ]
}